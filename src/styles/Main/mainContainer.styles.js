import styled from 'styled-components';
import { veryLightGrayBackground } from '../variables';

export const MainContainer = styled.div`
  margin-left: auto;
  margin-right: auto;
  background: ${veryLightGrayBackground};
  margin-top: 3.5rem;
`;
