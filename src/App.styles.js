import styled from 'styled-components';
import { nunitoSansFont, homePageFontSize } from './styles/variables';

export const GlobalStyles = styled.div`
  font-family: ${nunitoSansFont}
  font-size: 62.5%;
  margin-left: auto;
  margin-right: auto;
`;
